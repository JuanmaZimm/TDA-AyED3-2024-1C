Ejercico de BA MDQ

me dan una lista l de estaciones

G(L, C)
    idx = 0
    estaciones = 0
    for i in 1..n do
        if (E[i] - E[idx] > C) then
            idx = i-1
            estaciones = estaciones ++ {i}
        endif
    endfor
O(n)